{
  "name": "@ffcafe/ixion",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lefthook": "lefthook",
    "lint": "biome check --write .",
    "postinstall": "lefthook install",
    "test": "pnpm -r test",
    "x": "tsx packages/cli/src/index.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@tsconfig/node22": "^22.0.2",
    "@types/node": "catalog:",
    "lefthook": "^1.13.0",
    "tsup": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  },
  "workspaces": [
    "packages/*"
  ]
}
